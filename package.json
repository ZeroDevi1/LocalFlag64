{
  "name": "localflag64",
  "version": "1.1.0",
  "description": "Cloudflare Worker 版 Base64 订阅加旗服务",
  "type": "module",
  "private": true,
  "author": "LocalFlag64 contributors",
  "homepage": "https://github.com/ZeroDevi1/LocalFlag64",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/ZeroDevi1/LocalFlag64.git"
  },
  "scripts": {
    "build": "wrangler deploy --dry-run --outdir dist/worker",
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "test": "vitest run",
    "typecheck": "tsc --noEmit",
    "check": "npm run typecheck && npm run test && npm run build"
  },
  "devDependencies": {
    "@types/node": "^24.6.1",
    "typescript": "^5.9.3",
    "vitest": "^3.2.4",
    "wrangler": "^4.68.0"
  }
}
